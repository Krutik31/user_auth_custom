<div class="text-center">
  <% if user_signed_in? %>
    <h3>Welcome <%= current_user.username %>, You are logged in!</h3>
    <h5><%= link_to "Sign out...", session_path(current_user.id), data: {turbo_method: :delete} %></h5>
  <% else %>
    <h3>Kindly <%= link_to "Login", new_session_path %> or <%= link_to "Register yourself", new_registration_path %>...</h3>
  <% end %>
</div>